<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.fpi.mapper.user.UserMapper">
<!--    우선, 마이페이지에서 작성글 검색하려고 만들어둠, 미완성-->
    <select id="selectUserCommuList">
        select c.*,co.*,pr.*
        from TBL_COMMUNITY c JOIN TBL_COMMENT co ON c.USER_ID = co.USER_ID
                            JOIN TBL_PRO_REVIEW pr ON c.USER_ID =pr.USER_ID
        <where>
            <if test="search !=null">
                <choose>
                    <when test="searchType == 'title'"> c.community_title LIKE '%' || #{search} || '%'</when>
                    <when test="searchType == 'review'"> pr.PRO_REVIEW_TITLE LIKE '%' || #{search} || '%'</when>
                    <when test="searchType == 'content'"> co.COMMENT_CONTENT LIKE '%' || #{search} || '%'</when>
                </choose>
            </if>
        </where>
        order by

    </select>

</mapper>